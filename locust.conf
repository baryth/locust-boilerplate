# Default flags for `locust -f src/locustfile.py`.
# Override any of these on the command line, e.g. `-u 20 -t 30m`.

locustfile = src/locustfile.py
users = 5
spawn-rate = 1
run-time = 10m
headless = false
